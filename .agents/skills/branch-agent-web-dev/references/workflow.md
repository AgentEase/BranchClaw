# Web Project Notes

- Prefer the repo's existing package manager and dev script over inventing a new command.
- Keep the dev server detached in tmux so the operator can inspect or reuse the preview URL.
- Report the URL even if the task is incomplete; that still helps the operator inspect progress.
