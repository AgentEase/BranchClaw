# Fullstack Project Notes

- Prefer separate tmux windows and logs for frontend and backend so failures stay observable.
- Do not claim the stack is healthy if only the frontend started and the API is broken.
- When backend verification is easier than keeping a long-running process, capture a request
  output snippet and report that instead of forcing a permanent server.
